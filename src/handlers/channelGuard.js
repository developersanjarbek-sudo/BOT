// VIP Channel Guard - DISABLED
import logger from '../utils/logger.js';
// The VIP Chat now works inside the bot as a peer-to-peer anonymous chat.
// This file is kept for reference but is no longer actively used.

export const setupChannelGuard = (bot) => {
    // Channel guard logic has been removed.
    // VIP users now chat anonymously within the bot itself.
    logger.warn('⚠️ VIP Channel Guard is disabled. Using in-bot anonymous chat instead.');
};
